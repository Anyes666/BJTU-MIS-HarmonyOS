export interface MisWebParams {
  mode: string;
  target?: string;
  termCode?: string;
}

export interface CourseDetailParams {
  courseId: string;
}

export interface MailDetailParams {
  messageKey: string;
}
